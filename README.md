# Week 3 – Insurance Risk Analytics

## Setup
1. Clone repo
2. Install dependencies: pip install -r requirements.txt
3. Run notebooks in notebooks/

## Data Version Control
- Data tracked with DVC.
- Run dvc pull to retrieve the dataset.

## CI
GitHub Actions runs on every push (see .github/workflows/ci.yml).
